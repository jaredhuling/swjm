# Count Leading Zeros After Decimal Point

Used for adaptive step size computation. Returns the position of the
first non-zero digit after the decimal point.

## Usage

``` r
count_digits(num)
```

## Arguments

- num:

  A numeric scalar.

## Value

Integer count of leading zeros after the decimal point.
